
@{
    Layout = "_Layout";
}
@using DBShopSite.Entities

<div class="container">
    <div class="row">
        <div>
            @{await Html.RenderPartialAsync(@"~/Views/Tovars/Filter/Filters.cshtml", (List<Filter>)ViewBag.ListFilter);}
        </div>
        <div class="body-content">
            @RenderBody()
        </div>
    </div>
</div>